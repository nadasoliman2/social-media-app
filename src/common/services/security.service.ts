import{generateHash, compareHash,decrypt,encrypt} from '../utils/security/index.js'
export class SecurityServices{
    constructor(){

    }
generateHash = generateHash
compareHash = compareHash

decrypt = decrypt
encrypt = encrypt
}